#include <stdio.h>
#include <stdlib.h>

int main(void){

    int num1, num2, sum;

    printf("First number: %d", rand(num1) % 6 + 1 );
    printf("Second number: %d", rand(num2) % 6 + 1);

    sum = num1 + num2;

    if (num 1 == num2){

       printf("Sum = %d\n", sum = sum * 2);
    }
    else{
        printf("Sum = %d\n", sum);
    }

}